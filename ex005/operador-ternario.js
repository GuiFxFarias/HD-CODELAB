const hora = 16;

if (hora < 18) {
  console.log("Dia");
} else {
  console.log("Noite");
}

const hour = 18;

// Short if on the line below
const periodo = hour < 18 ? "Dia" : "Noite";

console.log(periodo);
