const paises = ["Brasil", "Argentina", "Uruguai"];

paises.unshift("Chile"); // add the value at the beginning of the array
console.log(paises);
paises.shift(); // remove value at the beginning of the array
console.log(paises);
